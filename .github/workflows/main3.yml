name: Context Example-3
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show GitHub Context
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref }}"
          echo "Actor: ${{ github.actor }}"
          echo "Event Name: ${{ github.event_name }}"

